{
  "name": "echoverse-ai-voice-journal",
  "version": "1.0.0",
  "private": true,
  "description": "An intelligent audio diary that turns your spoken thoughts into structured, actionable insights for self-discovery.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp manifest.json dist/ && cp sw.js dist/ && cp -r assets dist/ && cp -r src dist/"
  },
  "dependencies": {
    "@google/genai": "0.14.0",
    "date-fns": "3.6.0",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "recharts": "2.12.7"
  }
}